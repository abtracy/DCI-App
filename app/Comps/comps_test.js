'use strict';

describe('myApp.comps module', function() {

  beforeEach(module('myApp.comps'));

  describe('comps controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});